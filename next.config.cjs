/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'], // adicione todos os domínios externos que irá usar
  },
};

module.exports = nextConfig;
