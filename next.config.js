// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'], // Permite carregar imagens externas do Unsplash
  },
}

module.exports = nextConfig;